# Local dev: copy this file to .env and fill in your real Resend API key.
# .env is gitignored — never commit real secrets.
#
# Production (Vercel): this file is NOT read by Vercel. Set RESEND_API_KEY
# in the Vercel dashboard under Project Settings > Environment Variables instead.
RESEND_API_KEY=
